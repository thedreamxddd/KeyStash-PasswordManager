#ifndef CHECKFILE_H
#define CHECKFILE_H
#include <iostream>
#include <string>
#include <fstream>
#include <cstdio>
using namespace std;

bool FileExists(const string& tempfile);
bool FileDelete(const string& tempfile);

#endif
#pragma once
